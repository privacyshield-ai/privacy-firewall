# Environment Variables for Local LLM DLP

# Optional: Custom cache directory for transformer models
# Default: ~/.cache/huggingface/
# TRANSFORMERS_CACHE=/path/to/custom/cache

# Optional: Disable progress bars during model download
# HF_HUB_DISABLE_PROGRESS_BARS=1

# Optional: Use offline mode (requires models to be pre-downloaded)
# TRANSFORMERS_OFFLINE=1
